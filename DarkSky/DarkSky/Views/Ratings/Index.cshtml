@model IEnumerable<DarkSky.Models.RatingsCheckIn>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DarkSkyLocation.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Observer.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rating)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CheckIn)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DarkSkyLocation.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Observer.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rating)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CheckIn)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RatingsId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RatingsId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RatingsId })
        </td>
    </tr>
}

</table>
